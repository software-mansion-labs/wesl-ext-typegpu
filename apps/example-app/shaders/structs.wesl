struct StructA {
  a: vec3f,
}

struct StructB {
  a: vec3f,
}

struct StructC {
  a: vec3f,
}

struct StructD {
  a: vec3f,
}

struct StructE {
  a: vec3f,
}

struct StructF {
  a: vec3f,
}

fn function_to_ignore() -> vec2f {
  return vec4f(1.0, 0.0);
}
